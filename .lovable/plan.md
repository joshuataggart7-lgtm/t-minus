# Set-aside Word-face citation repair

## Scope
- Add the exact text `NFS CG 1815.28` to body prose retained in both successful and unsuccessful Word variants.
- Preserve the Sample 1 refusal, existing FAR citations, blank signature ink, officer name, filename, and all held document/form behavior.
- Verify extracted Word text, advance the release tip, and publish.

## Technical details
- Extend the existing marker-based Word master and marker map rather than adding a UI-only citation.
- Rebuild the master from the attached HQ binary, then exercise both variants through the existing generator and inspect their extracted text.
